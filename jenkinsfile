pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git branch: 'main', url: 'https://github.com/Naveen-12361/Two-Tier-Web-App.git'
            }
        }
        stage('Build') {
            steps {
                dir('two-tier-app') {
                    sh '/usr/bin/docker-compose build'
                }
            }
        }
        stage('Deploy') {
            steps {
                dir('two-tier-app') {
                    sh '/usr/bin/docker-compose down || true'
                    sh '/usr/bin/docker-compose up -d'
                }
            }
        }
    }
}
